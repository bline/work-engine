# Candidate implementation slices

These are later slice-supervisor planning inputs, not authorized work.

1. Inventory current selection, host receipt, episode, native binding, terminal receipt, campaign schemas/callers; map each consumed claim and owner; exclude PPCE-01/02.
2. Define versioned claim identity, evidence profile/reference, status, owner route, selection declarations, and compatibility rules.
3. Bind host observations to exact execution/selection/candidate/subject/session/realization/capability/continuity/integrity; add backend adapters without canonicalizing Claude artifacts.
4. Implement immutable normalized admission and Review Episode binding for recording-only, observation-only, combined, false, and unestablished cases.
5. Project terminal evidence and enforce fail-closed owner routing while keeping `reported` separate.
6. Add append-only correction/supersession, recovery, mixed-version handling, shadow migration audit, and truthful legacy classification.
7. Run deterministic end-to-end, restart, retention, compatibility, and negative-authority validation; produce a separate transition audit without self-acceptance.

A bootstrap slice may be proposed later only after exact Claude artifacts and separate protocol/provider authority exist.

Risks/dependencies: PPCE-02 or an accepted normalization owner may constrain vocabulary; host retention may be inadequate; embedding risks sensitive duplication while references risk availability; bad profiles may over-constrain or under-establish; mixed revisions need fencing; the broken path cannot self-certify; pre-S14 verticals are not S14 imports; the staging-path dogfood defect stays separately owned.
