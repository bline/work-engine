# Formation Evidence: Context-Decision Observability

## Evidence boundary

This file records attributed observations and formation inferences used for
`work-engine.context-decision-observability`. It does not own proposal meaning,
host behavior, role state, or an acceptance decision.

Repository cutoff: Work Engine
`cdc9e3fa5d300e5edc737faf38edf85a336fbdcf`, plus validated intake checkpoint
`2c9e4d85b4abb7830a3a607f93f7c5e821e8b031`.

Runtime observation date: 2026-08-24.

## Durable intake evidence

The validated intake record and projection preserve two independently
decidable candidates:

1. live context-decision observability for an operational Wind Walker
   consumer; and
2. context-replacement outcome evidence for a later research consumer.

The intake assigns continuation safety to Wind Walker and role-specific
durable meaning to role or domain owners. It excludes fixed reset thresholds,
automatic completeness proof, a universal role schema, and raw trace ownership
from the live candidate.

The intake checkpoint is an immutable formation input, not proposal acceptance
or implementation authority.

## Current Work Engine observations

- `skills/wind-walker/SKILL.md` defines continuation independence and leaves
  replacement benefit to model judgment.
- `work-engine.role-owned-durable-operational-state` is the neighboring formed
  candidate for role-owned recovery meaning and checkpoint semantics.
- `work-engine.raw-agent-execution-evidence-archive` is the neighboring formed
  candidate for optional forensic bytes and explicitly does not own operational
  recovery state.
- The bounded implementation search recorded by intake found no existing Work
  Engine projection for active context, effective hard limit, checkpoint
  baseline, post-checkpoint growth, and rehydration footprint.

These are repository observations at the stated cutoff, not accepted product
placement.

## Installed-runtime observations

- Installed command: `codex-cli 0.149.1`.
- The effective agent environment exposes `new_context` and
  `get_context_remaining`.
- In this formation session, `get_context_remaining` returned only an observed
  `tokens_left` value. The changing value is evidence that one pressure signal
  exists, not a durable semantic measurement for the proposal.
- `codex features list` reported `runtime_metrics` and `token_budget` as under
  development and disabled. `remote_compaction_v2` was reported stable and
  enabled.

These observations apply only to the installed configuration and current
session. Feature names, states, contracts, and exposed tools may change.

## Official OpenAI documentation observation

The official OpenAI model guidance available on 2026-08-24 describes Responses
API compaction as an opaque continuation mechanism for long-running workflows,
recommends monitoring context usage, and distinguishes compacted items from
inspectable application state:

<https://developers.openai.com/api/docs/guides/latest-model?model=gpt-5.2#4-compaction-extending-effective-context>

That documentation supports keeping compaction distinct from model-facing
semantic state. It does not establish the local Codex host's `new_context`
contract, effective invocation limit, window identity, feature availability,
or Work Engine ownership. Those remain separately attributed observations or
formation questions.

## Formation judgments

- A live observation packet has an independently decidable consumer and
  consequence from long-lived outcome research.
- Runtime adapters are the likely owners of direct context facts; role
  workflows remain the owners of continuation-complete checkpoint meaning.
- A composed projection can preserve those owners while giving Wind Walker one
  bounded surface.
- Forming the live candidate now preserves its decision boundary. Forming the
  outcome-research candidate now would prematurely choose identities and joins
  that the live vertical is intended to establish.
- The likely first slice-builder consumer is an evidence route, not permanent
  placement.

These judgments are revisable and carry no acceptance or implementation
authority.

## Advisory review and revision

The initial immutable review subject was checkpoint commit
`6eebd58acd991e62e6a85b5ac89de42f0712a74e`, tree
`7dddbc8b449df056b7923167f40ea121f5247828`, with task patch digest
`414c00adf90b490eb0c0fb2111db271fa176e8ae093597a856194fb47c842f92`.
Review selection, provider provenance, specialist projections, and synthesis are
stored under
[`reviews/proposals/context-decision-observability/6eebd58/`](../../../reviews/proposals/context-decision-observability/6eebd58/).

Three fresh Claude attempts failed before evidence because of provider quota.
Five fresh Codex specialist reviews then ran as
`accepted_same_model_review` with `independence_claimed: false` and retained
authority-bound remediation episodes. The panel advised revision rather than
rejection.

The revision incorporates these formation judgments:

- benefit evidence, safety attestation, situated judgment, effect authority,
  runtime transition, and durable episode history require separate owners;
- lifecycle and evidence identity must be exact enough to reject mixed-cutoff
  and cross-generation arithmetic;
- epistemic origin, acquisition method, access, validity, freshness,
  applicability, conflict, correction, and uncertainty are orthogonal;
- a projection must authenticate sources and consumers, enforce source-owner
  visibility, minimize model-visible metadata, and preserve fresh-reviewer
  isolation;
- a rehydration hash cannot confer instruction authority;
- a first builder vertical is feasibility evidence only and cannot prove
  open-world completeness, cross-role applicability, or permanent placement;
  and
- proposal evidence may be established by equivalent valid routes and cannot
  itself authorize an effectful context replacement.

These are proposal revisions under formation authority. Reviewer re-evaluation
remains advisory; neither the initial panel nor this response accepts the
candidate or authorizes implementation.
