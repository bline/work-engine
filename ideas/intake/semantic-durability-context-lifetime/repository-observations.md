# Repository observations for semantic-durability intake

Evidence cutoff: Work Engine `cdc9e3fa5d300e5edc737faf38edf85a336fbdcf`,
the exact raw-source checkpoint `3d5a7574f508fd5f3b5861dff348fcb5a6234846`,
and the later evidence checkpoint named by `record.json`.

## Current doctrine and proposal boundaries

- `skills/wind-walker/SKILL.md` owns continuation independence and leaves
  context-lifetime benefit to model judgment. It recognizes that carrying
  context can increase cost, but it does not currently articulate the raw
  idea's conditional-durability, opportunistic-preservation, reconstructability,
  or expected-lifecycle-cost semantics.
- The decided `work-engine.context-decision-observability` proposal owns an
  authenticated consumer-scoped observation projection. It supplies context
  pressure, lifecycle, checkpoint, and rehydration evidence without owning the
  situated judgment, replacement safety, effect authority, role state, or
  long-lived outcome research.
- `work-engine.role-owned-durable-operational-state` owns a stronger candidate
  publication boundary for resume-critical role meaning. Its requirement to
  publish when loss risks incorrect continuation, duplicate effects, lost
  authority, or material reconstruction cannot be relaxed by an economic
  preference to leave meaning transient.
- `ideas/bounded-ad-hoc-continuation-state.md` already captures the raw idea's
  bounded-memento consequence with a stable envelope, flexible attributed
  content, role ownership, safe rehydration, and evidence-driven schema
  promotion. Its exact relationship remains a later intake or formation
  judgment.

## Empirical and implementation evidence

- The earlier `work-engine.intake.state-complete-context-reset` assessment
  already preserves a separate `claim.context-reset-outcome-evidence`
  candidate. The decided observability proposal explicitly defers forming that
  longer-lived research owner until stable episode identities have been
  exercised.
- `work-engine.raw-agent-execution-evidence-archive` owns candidate forensic raw
  bytes and exact execution reconstruction, not operational memory or compact
  context-retirement policy.
- `work-engine.code-change-characterization-profile` provides a candidate
  immutable task/change control profile useful for comparing context policies
  without flattening heterogeneous engineering work.
- `harvest_codex_telemetry.py` currently derives cumulative input, cached-input,
  output, reasoning, turn-count, and wall-clock measurements from bound child
  rollouts. It deliberately reports `peak_context_tokens` as unavailable. These
  measurements do not establish active context occupancy, an invocation-
  effective hard limit, rehydration work, or the causal effect of a reset.
- During this intake, the active environment reported `86062` context tokens
  remaining. The capability exposes only `tokens_left` or unavailable; the
  observation does not establish the effective limit, active occupancy,
  provider billing cost, automatic-compaction state, or a comparable checkpoint
  baseline.

## Intake interpretation

The source contains two independently decidable surviving candidates:

1. a semantic-durability and context-lifetime judgment contract that can refine
   Wind Walker while consuming rather than owning the observability projection
   and role-state facts; and
2. an empirical calibration consequence that refines the previously deferred
   context-reset outcome candidate and may consume raw archive, change-profile,
   episode, cost, and later outcome evidence.

The bounded-memento section is preserved as unresolved relationship evidence
rather than duplicated into either candidate. The phrase describing the hard
context limit as a “safety boundary” must also remain unresolved: capacity is
an operational constraint and planning signal, while semantic safety remains
continuation independence and effect authority remains separately owned.

Codebase Memory Tier 2 searches used project
`home-bline-code-work-engine`, generation `2026-08-24T21:44:34Z`.
Task-directed graph search identified the current telemetry harvester and
continuation-context validator; direct source reads established their exact
behavior. Coverage checks for every relied-on path reported matching metadata
and no recorded issue. That is a best-effort signal, not proof of completeness.
