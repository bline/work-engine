import { ReviewerRuntimeError } from "../src/services/reviewer-runtime/contract.mjs";
export class ImplementationReviewerRuntime { constructor({manifest,adapter}) { if (!manifest?.projectRole || !adapter?.execute) throw new TypeError("reviewer runtime requires manifest and adapter"); this.manifest=manifest; this.adapter=adapter; } async review({instanceId,profileId,subject,catalogProjection,rawEventPolicy,continuationSessionId=null}) { const projection=this.manifest.projectRole("implementation-reviewer",instanceId); if (projection.role.threadOptions.sandbox!=="read-only" || projection.role.effects.length) throw new ReviewerRuntimeError("configuration","reviewer role exceeds read-only effect ceiling"); return this.adapter.execute({instanceId,profileId,subject,catalogProjection,rawEventPolicy,continuationSessionId,roleInstructions:projection.role.developerInstructions}); } async retire(instanceId) { return this.adapter.retire(instanceId); } }
