import { digest, ReviewerRuntimeError, validateReviewerProfile } from "./contract.mjs";
export class ReviewerProfileRegistry { constructor({profiles}) { if (!Array.isArray(profiles) || !profiles.length) throw new ReviewerRuntimeError("configuration", "registry requires profiles"); this.profiles = new Map(); for (const value of profiles) { validateReviewerProfile(value); if (this.profiles.has(value.profileId)) throw new ReviewerRuntimeError("configuration", `duplicate profile ${value.profileId}`); this.profiles.set(value.profileId, Object.freeze(structuredClone(value))); } this.revision = digest([...this.profiles.values()].sort((a,b) => a.profileId.localeCompare(b.profileId))); } admit(profileId) { const profile = this.profiles.get(profileId); if (!profile?.enabled) throw new ReviewerRuntimeError("configuration", `profile ${profileId} is not admitted`); return Object.freeze({profile, registryRevision:this.revision}); } }
