# Idea: explain stopped automation

When an automated engineering run stops, the operator sees a terminal label but
often cannot tell which unresolved decision or missing authority prevented
continuation. Preserve a compact explanation that a fresh operator can inspect
without replaying the run transcript.

This idea is speculative. It does not choose an implementation owner, authorize
work, or establish that a new proposal is preferable to existing machinery.
