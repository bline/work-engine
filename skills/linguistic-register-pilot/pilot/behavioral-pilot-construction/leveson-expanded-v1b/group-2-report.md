# Expanded Leveson v1b treatment-validation receipt

Experiment `leveson-expanded-v1b-treatment-validation-2026-08-26` is a fresh
attempt under a preregistration frozen before either new model judgment. It
does not rewrite or salvage the failed v1 result.

## Feature-evidence disposition

All four under-supported realization features acquired honest support from a
third distinct document in the already bound eight-document corpus:

- `lexicon-abstract-relational`: added `safety-iii-healthcare`;
- `lexicon-diagnostic-deficit`: added `safety-iii-healthcare`;
- `syntax-stacked-subordination`: added `systems-theoretic-software-intensive`;
- `rhythm-telegraphic-prompt-response`: added `software-system-safety-notes`.

The ≥3-document rule was unchanged. All four remain retained. Their source
observations were rerun through the quotation audit with the rest of the
profile.

## Exact gates

The corpus still contains eight sole-authored/presented documents across six
genre classes, balanced 4/4 between formal-analysis and practitioner/public
families, with 73,794 normalized tokens. The profile retains 8 of 16 features,
44.06779661% of frozen weight, and seven cross-family features. Every retained
feature now cites at least three documents.

Quotation survival passes: the longest normalized exact source/profile overlap
is four tokens, with zero material exact matches and zero near-verbatim
matches.

The four fresh cards each contain five features, the common card-local salience
multiset `[4, 4, 3, 3, 3]`, and exactly 72 normalized words. Layer composition
preserves comparator availability and remains visible to the baseline. The
exhaustive preregistered single-scalar baseline found zero cross-replica-perfect
Leveson rules.

Fresh same-family recognition assigned expanded Leveson correctly. Total
matching was 2/4: the neutral and Leveson cards were correct, while Gelman and
Shaw were swapped. The recognizer also described an unrequested three-point
confidence scale, so its uniform score of 2 is retained only as uncalibrated
provenance. The frozen gate required the expanded-Leveson assignment,
not perfect comparator matching, so recognizability passes with the comparator
ambiguity retained as a limitation.

## Disposition

All frozen v1b treatment-validation gates pass. The expanded Leveson treatment
is ready for the separately bounded behavioral-contract construction stage.
This receipt does not authorize behavioral execution. No behavioral task,
scoring key, C0/C1/C2 role artifact, or behavioral outcome was authored, read,
or generated in this group.

Source bytes, the source-bearing evidence packet/result/event stream, and the
recognition event stream remain outside Git under the content-retention cache.
Committed reports retain their digests and derived non-source-bearing evidence.
