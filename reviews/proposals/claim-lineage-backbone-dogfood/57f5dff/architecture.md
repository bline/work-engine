# Architecture Review Continuation

## Role and boundary

Retained-context continuation of the architecture and experimental-placement
review for `work-engine.claim-lineage-backbone-dogfood`. The exact remediated
subject is private checkpoint
`57f5dffeb7ae38fe6df80c2029de5bc91a85adba`, tree
`c0589f3b4d76efcd180b2943e43e1f20fd7ed12d`. This pass reassesses only my prior
architecture findings against the bounded `proposal.md` and `placement.md`
delta. It is not a fresh or independent review and grants no proposal,
placement, fixture, schema, or implementation authority.

## Prior-consequence disposition

- Filename adjacency and reversible-removal advisory: **discharged**.
- Preliminary-placement-evidence limitation: **remains advisory by design**.
- Provisional ownership and canonical-history/query separation: **remain
  coherent; not reopened**.

## Continuation readiness

`ready_for_authority_decision`

No architecture or placement finding warrants another proposal revision. The
remediation strengthens experimental isolation and fixture authority without
selecting a shared owner, production schema, permanent store, or runtime
boundary.

## Findings

### 1. The adjacency/reversibility advisory is discharged

Severity: none remaining.

The placement now records the actual recursive `packet.json` discovery hazard,
requires an unmistakably experimental namespace and non-colliding filenames,
and requires a disposable-checkout demonstration that adding and removing the
experimental machinery leaves proposal-packet and review-artifact meaning and
validation unchanged. It separately permits historical evidence to remain only
with explicit ownership and non-production status
(`proposals/evidence-lineage/claim-lineage-backbone-dogfood/placement.md:22-29`).

Consequence: the experiment is reversibly adjacent rather than implicitly
inside the proposal-packet owner. The retained evidence/machinery distinction
also avoids equating reversibility with destructive loss of experimental
history.

### 2. Expanded fixture authority does not create a shared production owner

Severity: none.

The new fixture contract binds original-judgment, claim-boundary
interpretation, refresh, reliance, and reopening authority for each research
and review fixture. Missing authority blocks a positive proof, and explicitly
experimental refresh authority can establish only a dogfood-local judgment;
changing the canonical proposal or review conclusion still requires separate
source-domain authority
(`proposals/evidence-lineage/claim-lineage-backbone-dogfood/proposal.md:143-162`).
The unchanged artifact boundary continues to make existing packets, reviews,
decisions, receipts, checkpoints, and Git objects canonical for their own facts
while the experiment owns only its local representation (`proposal.md:176-198`;
`placement.md:31-43`).

Consequence: the added authority matrix makes the provisional ownership model
more explicit; it does not establish a permanent evidence-lineage owner or let
execution authority leak into domain judgment.

### 3. The canonical-history/query split remains coherent

Severity: none.

The projection remains rebuildable, source-identified, and completeness-bound
(`proposal.md:176-192`; `placement.md:10-15`). The remediated validation
consequence now binds the rebuild to an exact canonical-input manifest,
schema/build version, cutoff, exclusions or failures, and output digest
(`proposal.md:266-279`). These additions make projection provenance stronger
without promoting the projection into canonical meaning or choosing its store.

Consequence: no newly introduced schema, storage, or runtime commitment was
found.

### 4. Placement evidence remains deliberately preliminary

Severity: advisory.

Both real fixtures must now exercise the shared core, with an identity-fork
counterfixture and a closed proof-coverage matrix (`proposal.md:61-104`,
`:164-174`). Valid execution is also separated from a positive result, so a
negative, incomplete, ambiguous, or authority-blocked run remains truthful
evidence instead of being repaired through fixture substitution
(`proposal.md:47-59`, `:266-279`). This improves the experiment's ability to
produce useful placement evidence.

The original limit still holds: two bounded fixtures cannot certify a permanent
shared owner or discharge the parent lifecycle and recovery boundary. The
remediated proposal continues to state that permanent placement, projection
storage, production boundaries, and the larger dogfood matrix remain deferred
(`proposal.md:223-242`, `:292-299`; `placement.md:72-85`). This is an explicit
scope limit, not an unresolved contradiction.

## New-consequence assessment

The principal new architecture consequence is the introduction of explicitly
experimental refresh authority. It is coherently placed because it is
fixture-local, must be effective for the exact claim and outcome, cannot alter
the source-domain conclusion, and cannot be inferred from the execution
disposition (`proposal.md:145-162`, `:281-290`). No accepted owner is altered.

## Limitations and evidence provenance

- This continuation did not select fixtures, establish any authority grant, or
  execute the removal/projection proofs.
- The two changed worktree files exactly matched checkpoint `57f5dff`; their
  SHA-256 values matched the continuation subject, and the checkpoint diff from
  the prior subject contained only those two paths.
- Verify-tier Codebase Memory coverage at generation
  `2026-08-23T01:57:17Z` reported `no_recorded_issue` and matching metadata for
  the continuation subject, my prior artifact, and both changed paths. This is
  best-effort coverage, not proof of completeness. Exact prose and the
  checkpoint delta were read directly.
- Persistence of this continuation is not new evidence and does not exercise
  proposal or implementation authority.
