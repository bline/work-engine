# Authority and Doctrine Review

Disposition: revise before acceptance. Durable episode revision:
`ebe5a706e721a9966e803a67c01380826b2de3f5`.

- `CDO-AUTH-001` (high): canonical doctrine owns the safety invariant, Wind
  Walker projects it, the currently authorized role/model owns the situated
  judgment, and the runtime acts only under a separately verified current
  grant. A skill or packet is not a decision authority.
- `CDO-AUTH-002` (high): proposal formation does not authorize the first
  vertical's effectful reset. Move any such experiment to a separately
  authorized evaluation plan and state acceptance evidence as observable
  consequences with equivalent valid routes.
- `CDO-AUTH-003` (high): durable references and historical measurements need
  exact provenance, freshness, applicability, authority class, and permitted
  use. The packet must say explicitly that it does not establish safety.
- `CDO-AUTH-004` (medium): first-profile evidence cannot silently generalize to
  human-facing, reviewer-isolated, or visibility-constrained roles. Scope any
  decision to exercised role classes or require an explicit profile contract
  for every consumer.

The review is advisory and does not choose permanent architecture or authorize
an experiment.
