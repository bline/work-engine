# Lifecycle and Evidence Review

Disposition: block proposal acceptance and an effectful first vertical pending
revision and re-review. Durable episode revision:
`ae95e6236c2c40ac39a9e5b519f4cc9921a861d5`.

- `CDO-LC-001` (high): represent composite runtime/session/worker/context and
  active-representation generations, predecessor and transition identity, and
  compaction policy revision. Separate semantic checkpoint applicability from
  quantitative baseline comparability.
- `CDO-LC-002` (high): give the packet an identity, revision, and coherent
  snapshot cutoff; bind every material field and derivation to exact source
  observations and fail unavailable or conflicting when coherence is absent.
- `CDO-LC-003` (high): integrity-bind the manifest, ordered entries, loading
  modes, estimator basis, transition, before/after generations, and actual load.
  A stronger durable episode owner must retain this join.
- `CDO-LC-004` (high): a builder cannot prove its own open-world continuation
  completeness. Use an externally owned oracle or seeded obligation and report
  success only as bounded falsification.
- `CDO-LC-005` (medium): provenance, availability/access, freshness,
  applicability, conflict, correction, and supersession are orthogonal. Current
  projection state must expose invalidation without becoming the history owner.
- `CDO-LC-006` (medium): bind effective-limit claims to exact provider, model,
  invocation, configuration/catalog revision, and acquisition mode; reject an
  advertised theoretical maximum as an invocation-effective observation.

Unresolved implementation evidence includes the authoritative runtime rule for
generation advancement on compaction and the concrete stronger owner for a
future evaluation episode. Those uncertainties do not require choosing either
answer during proposal formation.
