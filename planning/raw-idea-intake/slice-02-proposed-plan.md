# Slice 2 proposed plan v2: Bounded Intake-Projection Formation Dogfood

- Run: `5a74bd5c-3ebc-4e6e-9d93-07dd96aede5a`
- Slice: `2`
- Attempt: `2`
- Status: `proposed_v2`
- Workflow route: `falsified-placement`
- Placement risk: `medium`
- Placement verdict: `confirmed`
- Procedural auto-approval eligible: `yes`
- Repository mutations during planning: this planning artifact only

## Goal

Exercise a fresh proposal former using only one owner-produced bounded intake
projection and its named evidence. Preserve truthful zero-, one-, and
multiple-proposal outcomes, validate every surviving proposal through the
existing packet contract, publish complete formed meaning at an exact immutable
Git checkpoint before review, and prove a fresh consumer can recover the
material proposal meaning without the raw idea collection or formation
conversation.

## Accepted predecessor and configuration

The exact semantic predecessor is
`refs/work-engine/checkpoints/5a74bd5c-3ebc-4e6e-9d93-07dd96aede5a/slice-1/accepted`:

- lifecycle commit: `af7c185318013041c4549a1165a942d9ee689e99`
- tree: `1e88831962c06f9e90b03c5d38719a4993a634c5`
- task patch: `1ab66bf41d168eb478cd0faed68dc4fdb0ed7c6a154864fab138b14c18dfcb3c`
- checkpoint identity:
  `c7ac637abb17d3f441295c7d92473f514c3a6f609d098a212c204cf9d8d9eb4b`

Current branch HEAD independently contains unrelated Wind Walker work. It is
not part of this slice and must not be absorbed, overwritten, or attributed to
it.

Campaign configuration version 2 is bound to campaign SHA
`7983910fd3e0faffc5b37377a344fd6193ef42858d8f0b5cfe02b015b9bcb62c`.
Repository retrieval remains `codex-codebase-memory` through `repo-search`.
The human-authorized temporary review role is Codex `gpt-5.6-sol` low through
`codex-adversarial-review`, evidence class `accepted_same_model_review`, with
fresh-process isolation and `independence_claimed: false`. The authorization
artifact SHA is
`8ab9f94c2ccdad6c4aaaf43570e7d01d3f5edb56c32331daef8bf3aae96da7fa`.

## Route revision from proposed v1

- **Failed premise:** Truthful cardinality and immutable publication require a
  new proposal-former JSON schema, runtime validator, and publication verifier.
- **Stale decisions:** The proposed
  `formation-result-v1.schema.json`, `proposal_formation.py`, and closed terminal
  result envelope are removed from scope.
- **Preserved evidence:** Proposal-former owns formation judgment; intake owns
  the bounded projection; proposal-packets owns surviving packet identity and
  closed mechanics; immutable formed meaning must precede review; static
  packet-only fixtures are insufficient.
- **Replacement route:** Add a behavioral vertical under proposal-former that
  consumes an actual intake projection, represents zero with an attributed
  durable prose disposition rather than a fake packet, represents one/multiple
  with complete packet fixtures, and reuses packet validation plus immutable
  slice-checkpoint mechanics.
- **Reason:** The fresh Sol Low falsifier confirmed the semantic owner but found
  no current consumer requiring a second closed formation-state owner. Existing
  packet and checkpoint machinery already establishes the mechanical
  publication consequence.

## Bounded scope

1. Clarify the proposal-formation contract only as needed to state that a
   completed zero-proposal outcome leaves an attributed durable disposition
   sufficient to distinguish semantic decline from absence, interruption, or
   failure. Do not prescribe a universal schema or formation procedure.
2. Build a temporary Git-backed test repository from the accepted idea-intake
   profile, validate the intake record, and call the owner implementation's
   `build_projection` function. Formation receives that projection and only
   evidence named by it; it does not receive the raw idea collection or prior
   assessment conversation.
3. Launch one fresh read-only proposal-former process with no inherited builder
   history and give it only the serialized projection plus the evidence named
   by that projection. It returns a bounded semantic formation artifact for the
   builder to materialize; it receives no repository mutation or review
   authority. This actual dogfood outcome does not substitute for the
   deterministic cardinality cases below.
4. Exercise three bounded outcomes:
   - zero: an attributed human-readable formation disposition and no
     `packet.json`;
   - one: one complete packet whose origin resolves to the bounded projection;
   - multiple: two independently decidable complete packets, with explicit
     split reasoning, stable relationships, and shared projection provenance.
5. Validate the one- and multiple-packet repositories with the existing
   proposal-packets capability. Do not ask proposal-packets to validate or own
   the zero outcome.
6. Use the existing slice-checkpoint capability after deterministic gates to
   create the exact immutable Git subject. Before semantic review, archive that
   exact commit and rerun packet validation against the archive.
7. Give a fresh intended consumer only the immutable bounded formation state
   and require it to recover candidate consequence, boundary/placement,
   relationships, uncertainty/evidence needs, source provenance, and explicit
   non-authorization without reconstructing formation.

This slice does not add a production intake record, a production dogfood
directory, a formation runtime/schema owner, a publisher, proposal evaluation
or decision semantics, permanent physical placement, claim-lineage
integration, cleanup, UI/inbox, scheduling, or continuous assessment.

## Exact provisional task-owned path set

Baseline overlaps:

- `skills/proposal-former/references/formation-contract.md`
- `skills/proposal-former/tests/test_proposal_former.py`

New fixture paths:

- `skills/proposal-former/tests/fixtures/intake-formation/zero/disposition.md`
- `skills/proposal-former/tests/fixtures/intake-formation/one/packets/cache-invalidation/packet.json`
- `skills/proposal-former/tests/fixtures/intake-formation/one/packets/cache-invalidation/proposal.md`
- `skills/proposal-former/tests/fixtures/intake-formation/multiple/packets/cache-invalidation/packet.json`
- `skills/proposal-former/tests/fixtures/intake-formation/multiple/packets/cache-invalidation/proposal.md`
- `skills/proposal-former/tests/fixtures/intake-formation/multiple/packets/cache-observability/packet.json`
- `skills/proposal-former/tests/fixtures/intake-formation/multiple/packets/cache-observability/proposal.md`

The test may serialize the actual runtime projection into its temporary Git
repository; that generated temporary file is not a repository task-owned path.
No changes are planned beneath `skills/idea-intake/`,
`skills/proposal-packets/`, `skills/slice-checkpoint/`, or a production proposal
directory. If implementation shows one of those owners cannot support the
certificate, return a boundary-change request instead of expanding silently.

## Confirmed placement certificate

- **Trigger:** An authorized fresh proposal former receives one validated
  bounded intake projection and only evidence named by that projection.
- **Producer:** `skills/proposal-former` challenges, declines, preserves, or
  splits surviving candidate meaning and authors the terminal semantic outcome.
- **State owner:** For surviving candidates, proposal manifests own stable
  identity and closed lifecycle metadata and their narratives own formed
  meaning. When no candidate survives, an attributed durable formation
  disposition distinguishes semantic completion from absence or failure without
  creating proposal identity or a second closed runtime owner.
- **Consumer:** Proposal-packets validates each surviving packet mechanically;
  a fresh evaluator/reviewer consumes the exact immutable checkpoint revision.
- **Lifecycle:** Validate and project one immutable intake subject; form zero,
  one, or multiple outcomes; validate every surviving packet; create and
  validate the exact checkpoint; revalidate archived packet bytes; only then
  begin semantic review.
- **Semantic consequence:** A fresh consumer can distinguish no proposal from
  one or several independently decidable formed candidates and recover their
  material meaning without rereading the raw collection, reconstructing the
  intake conversation, or treating intake judgment as proposal authority.
- **Downstream proof:** The Git-backed vertical consumes the actual owner-built
  projection, proves all three cardinalities, resolves every packet origin to
  the projection/source identity, validates packet repositories, and proves
  fresh-consumer usability from the exact archived checkpoint subject.
- **Insufficient substitute:** A static projection lookalike, raw-idea-only
  fixture, producer-only handoff, fake packet for zero, packet validation
  without formation-input binding, mutable working-tree review, or a new schema
  whose consumer and ownership necessity have not been demonstrated.

## Rejected alternatives

- **Idea-intake owns formation result:** rejected because intake owns
  source-bound assessment and projection, while proposal formation decides
  whether candidate meaning becomes proposals.
- **Proposal-packets owns formation result/publication:** rejected because it
  owns closed mechanics for surviving packets, cannot represent a valid empty
  packet repository, and has no formation authority.
- **New result schema/script/publication verifier:** rejected because no current
  consumer needs those distinctions mechanically; it would create redundant
  durable state and prematurely settle richer runtime structure.
- **Production dogfood artifact:** deferred because the accepted implementation
  plan places materially varied workflow dogfood and permanent-placement
  learning after this bounded handoff proof.
- **Static packet-only fixtures:** rejected because they cannot show consumption
  of the owner-produced intake projection or fresh-consumer usability.

## Invariants and risks

- Mechanical validity does not establish semantic correctness, human authority,
  proposal value, acceptance, priority, or implementation authorization.
- Zero must remain distinguishable from non-execution without manufacturing a
  proposal identity.
- Multiple packets from one projection must preserve explicit split meaning and
  valid stable relationships rather than duplicate or unrelated identities.
- Packet validation and fresh-consumer semantic evidence establish different
  claims; neither substitutes for the other.
- The checkpoint is an immutable locally reachable Git review subject, not a
  claim of remote publication.
- Normative contract changes require revision-bound agent-instruction review.
- Same-model review is truthfully labeled accepted same-model review and never
  independent review.

## Implementation order and boundary check

1. Establish the actual projection before contract prose or fixture polish.
2. Run the fresh proposal-former dogfood process from only that projection and
   named evidence, then establish the zero/one/multiple assertions around the
   attributed artifact and bounded contract cases.
3. If the actual projection cannot supply the consumer evidence or packet origin
   binding required by the certificate, stop with a boundary-change request.
4. Add only the minimum contract clarification and fixture meaning needed by the
   passing vertical.
5. Stop before the configured final gate with an exact task-owned manifest and
   baseline-overlap report.

## Validation mapping

### `semantic_proof`

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest skills/proposal-former/tests/test_proposal_former.py -v
```

The vertical must prove actual intake projection consumption, named-evidence
confinement, zero/one/multiple outcomes, projection/source origins, complete
consumer fields, packet non-authorization, and later immutable-subject use.

Focused packet mechanics:

```bash
python3 skills/proposal-packets/scripts/proposal_packets.py validate skills/proposal-former/tests/fixtures/intake-formation/one/packets
python3 skills/proposal-packets/scripts/proposal_packets.py validate skills/proposal-former/tests/fixtures/intake-formation/multiple/packets
```

### `risk_proportional_checks`

```bash
python3 /home/bline/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/proposal-former
PYTHONDONTWRITEBYTECODE=1 python3 -m py_compile skills/proposal-former/tests/test_proposal_former.py
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/idea-intake/tests -p 'test_*.py' -v
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/proposal-former/tests -p 'test_*.py' -v
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/proposal-packets/tests -p 'test_*.py' -v
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/slice-checkpoint/tests -p 'test_*.py' -v
```

After deterministic checks:

1. Run revision-bound `agent-instruction-review` on the exact contract change.
2. Create and validate the immutable candidate checkpoint using
   `slice-checkpoint`; do not change the user's branch, index, or working tree.
3. Extract the exact checkpoint commit into a temporary directory and rerun the
   one/multiple packet validation commands against that archive.
4. Run one fresh consumer usability check from only the archived bounded state.
5. Start one fresh authority-bound Codex `gpt-5.6-sol` low adversarial-review
   episode under `codex-adversarial-review`. Record evidence class
   `accepted_same_model_review`, fresh-process isolation, no inherited builder
   context, same-model relationship, and `independence_claimed: false`. Retain
   that reviewer through ordinary remediation.

The planning falsifier is not the gate reviewer and supplies no review
acceptance.

### `workspace_integrity`

```bash
git diff --check -- skills/proposal-former
```

Compare the exact task-owned manifest against accepted predecessor
`af7c185318013041c4549a1165a942d9ee689e99`, confirm that the two baseline
overlaps contain only attributed slice edits, and prove no unrelated current
work changed.

### Immutable-revision proof after checkpoint creation

Use an explicit temporary directory, the exact returned checkpoint commit OID,
and the archived repository's own packet validator. Do not use an unresolved
environment variable or mutate the working repository:

```bash
git cat-file -e '<checkpoint-commit-oid>^{commit}'
FORMATION_ARCHIVE_DIR="$(mktemp -d)"
git archive '<checkpoint-commit-oid>' | tar -x -C "$FORMATION_ARCHIVE_DIR"
python3 "$FORMATION_ARCHIVE_DIR/skills/proposal-packets/scripts/proposal_packets.py" validate "$FORMATION_ARCHIVE_DIR/skills/proposal-former/tests/fixtures/intake-formation/one/packets"
python3 "$FORMATION_ARCHIVE_DIR/skills/proposal-packets/scripts/proposal_packets.py" validate "$FORMATION_ARCHIVE_DIR/skills/proposal-former/tests/fixtures/intake-formation/multiple/packets"
```

The temporary directory is cleanup-only host state and is not a task-owned
repository path.

## Validation breadth

Selected stages:

- actual-projection vertical semantic proof;
- explicit zero/one/multiple outcome tests;
- focused intake, formation, packet, and checkpoint regressions;
- packet validation in the working candidate and archived immutable subject;
- skill validation and Python compilation;
- workspace and baseline integrity;
- revision-bound agent-instruction review;
- fresh consumer usability evidence;
- fresh accepted same-model adversarial review with retained remediation.

The full repository regression suite and generated-artifact freshness checks
are initially omitted because this slice changes no shared runtime, production
registration, persistence owner, generated artifact, or sibling capability.
Add broader checks if implementation crosses that boundary or focused evidence
shows wider coupling.

## Deferred scope and open decisions

- materially different representative intake subjects;
- migration of one manual reconciliation case;
- permanent record placement and production dogfood storage;
- shared claim-lineage integration;
- richer resumable formation or assessment runtime state;
- proposal review/evaluation/decision/priority/implementation semantics;
- raw-source cleanup, movement, redirect, merge, or deletion;
- UI/inbox, scheduling, and continuous assessment.

No human product, authority, or architecture decision remains open for this
slice. Exact zero-disposition prose and model formation route remain judgment
inside the accepted ownership boundary. Repeated dogfood may later establish a
real consumer for closed formation-episode state; this slice must not assume
that future decision.

## Evidence and coverage cutoff

- Verify tier, graph project `home-bline-code-work-engine`.
- Current full generation `2026-08-24T09:11:43Z`, complete, with no
  parse-partial or skipped files.
- The fresh Sol Low targeted falsifier inspected the accepted predecessor
  directly for contract/literal claims because the graph reflects current
  `main` at `900cef6263601569bdb1d185d3aabf7d88c2ecf5`.
- All material evidence paths reported `no_recorded_issue` with metadata match
  at the recorded generation. The final planning-file edit then reported
  `metadata_changed`; that authored file was read directly as the required
  source fallback.
- Checked scopes for proposal-former, idea-intake, proposal-packets,
  slice-checkpoint, raw-intake proposal, and interactive-formation proposal.
  The only gaps were excluded `__pycache__` directories, which contain no
  relevant source or contract evidence.
- Coverage is best-effort and is not proof of completeness.

## Next lifecycle transition

The revised plan is eligible for `procedural_auto_approval` under the configured
`procedural_when_safe` policy. The supervisor should validate and persist this
exact proposed-v2 plan as the accepted slice-2 plan, then return the accepted
scope and placement certificate to this same builder identity for the separate
implementation turn. `approval.uninterrupted_after_plan` is false, so plan
acceptance does not authorize implementation and gate in this turn.
